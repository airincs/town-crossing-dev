import React, { FC } from "react";
import Note from "./Note/Note";

const Bulletin: FC = () => {
  return (
    <React.Fragment>
      <div>Bulletin</div>
      <Note />
    </React.Fragment>
  );
};

export default Bulletin;
